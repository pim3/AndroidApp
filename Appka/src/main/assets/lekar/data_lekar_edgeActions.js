/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_next_to_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh1}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_back_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 31250, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 25000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 19500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 10500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 34250, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 59500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 69000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next_to_4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next_to_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next_to6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next_to_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("vlozka");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("vlozka");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_next_to_8}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_8");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh8}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_8");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 33500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_left_to_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_refresh_vlozka}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("vlozka");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_l_right_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

})(jQuery, AdobeEdge, "EDGE-zber_dat");