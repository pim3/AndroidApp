/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      
      
      
      
      
      
      Symbol.bindElementAction(compId, symbolName, "${_back_to_main}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start");

      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 12500, function(sym, e) {
         // insert code here// play the timeline from the given position (ms or label)
         sym.play("start_loop1");
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_3}", "click", function(sym, e) {
         // insert code for mouse click here// play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("continue");
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_bact_to_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start__slide_2");
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_3b}", "click", function(sym, e) {
         // insert code for mouse click here// play the timeline from the given position (ms or label)
         sym.play("start_3b");
      
      
      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 16000, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("loop_kruzok");
         

      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 23309, function(sym, e) {
         /*
         sym.stop();
         setTimeout(function(){sym.play("loop_start_3b");},2000);
         */
         
         sym.play("loop_start_3b");
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_bact_to_3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
      
      
      });
      //Edge binding end
      
      
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_4}", "click", function(sym, e) {
         // insert code for mouse click here
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_back_to_3b}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3b");
      
      
      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 30843, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("loop_start_4");
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
      
      
      });
      //Edge binding end
      
      
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 36488, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("loop_start_beat");
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_bact_to_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_to_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
      
      
      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 41000, function(sym, e) {
         sym.stop();
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
      
      
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_back_to_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 45000, function(sym, e) {
      
      
      });
      //Edge binding end
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 44019, function(sym, e) {
         // insert code here
      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_next_finall}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_finall");
      
      
      });
      //Edge binding end
      
      
      
      Symbol.bindElementAction(compId, symbolName, "${_refresh5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
      
      
      });
      //Edge binding end
      
      
      
      Symbol.bindElementAction(compId, symbolName, "document", "compositionReady", function(sym, e) {
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_back_to_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         // insert code for mouse click here

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_next_slide_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("continue");
         // insert code for mouse click here

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start");
         // insert code for mouse click here

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_back_to_4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         // insert code for mouse click here

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 11500, function(sym, e) {
         // insert code here
      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 8000, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start__slide_2");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_next_from_continue}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start__slide_2");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6750, function(sym, e) {
         // insert cosym.stop();
         sym.stop();

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 22000, function(sym, e) {
         /*
         var counter = sym.getVariable("counter");
         //set the value of a Symbol variable
         //sym.setVariable("counter", counter+"1");
         counter=counter+1;
         
         
         
         sym.stop();
         setTimeout(function(){sym.play();},2000);
         */

      });
      //Edge binding end

      

      

      Symbol.bindElementAction(compId, symbolName, "${_pause}", "click", function(sym, e) {
         sym.stop();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("pause").hide();
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("play").show();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_play}", "click", function(sym, e) {
         sym.play();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("play").hide();
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("pause").show();
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 63000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_bact_to_continue}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("continue");
         

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'package'
   (function(symbolName) {   
   
   })("package");
   //Edge symbol end:'package'

   //=========================================================
   
   //Edge symbol: 'fig1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 12500, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("loop_start");

      });
      //Edge binding end

   })("fig1");
   //Edge symbol end:'fig1'

   //=========================================================
   
   //Edge symbol: 'Preloader'
   (function(symbolName) {   
   
   })("Preloader");
   //Edge symbol end:'Preloader'

})(jQuery, AdobeEdge, "EDGE_tlakomer_meranie");