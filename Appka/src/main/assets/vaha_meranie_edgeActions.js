/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_10");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_0}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_0");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4901, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_0}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_1");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_1}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_0");
         

      });
      //Edge binding end

      

      Symbol.bindElementAction(compId, symbolName, "${_right_1}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_1");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_2}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 14882, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 15000, function(sym, e) {
         // insert code here
      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_2");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_3}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 23500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_4}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_3");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 42750, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_4");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_5}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 58750, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 59250, function(sym, e) {
         // insert code here
      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_5");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_6}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 76250, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_8");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_7}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_6");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 94000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_8}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_8");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_8}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_9");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_8}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_7");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 107500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_9}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_8");
         

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 112250, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_10}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_10");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_10}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_11");
         

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 142500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_right_11}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_12");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_11}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_11");
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_11}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_10");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 152000, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_refresh_12}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_12");
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_left_12}", "click", function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play("start_11");
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 162500, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'Symbol_1'
   (function(symbolName) {   
   
   })("Symbol_1");
   //Edge symbol end:'Symbol_1'

})(jQuery, AdobeEdge, "EDGE-vaha");